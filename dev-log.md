实验样本准备:new1 改词 / new2 头部插段 / new3 移动第 12 段至第 1 段后。

---

## V2-A: 自研词级 LCS(1.1–1.4)

**分词器的两个坑:**
1. `isalnum()` 对汉字也返回 True——内层吃词循环必须排除 CJK,否则
   `"extract_paragraphs函数"`(无空格)会被吞成一个 token。写成回归测试了。
2. 全角字母数字(ＡＢＣ１２３)同样被 isalnum 命中,靠"先判 CJK 再判 isalnum"的顺序挡住。

**换算法引入的碎片问题:** v1 的 difflib 天然输出块状 opcodes;token 级 LCS 会产出
几十个碎片({"delete":"讲"},{"delete":"问"},{"delete":"题"})。必须合并相邻同 tag 项,
否则渲染出来是一串闪烁的色块。"算法更细"和"输出更好"不是一回事,中间隔着一层呈现设计。

**快照预期红但没红(如实记):** 手册预测换 inline 算法后快照必红,实际 new1 那处修改的
golden 与词级输出字节级一致。原因:纯中文就近删除("新鲜烘焙"→"新")时,字符级 LCS
和词级 LCS 的重合率极高——这恰好实证了 1.1 的分词取舍结论("中文逐字的 LCS 结果在视觉上
已经接近词级,真正的收益差距远小于直觉")。词级真正的差异只出现在拉丁文本
(function→functions 整词替换,difflib 会碎成 equal+"s")和跨词边界的部分匹配上,
这两类已被 test_inline.py 显式覆盖。

**1.4 性能兜底(两级细化 + 规模上限):** 2000 字段落直接跑词级 LCS 需要 400 万格 dp 表,
先按句对齐后降到 200×200 加句内小表,实测 0.02s。全文一句(切句切不开)时句级路径
无解,退回 difflib——"没救"的输入也不许卡死。性能表已进 README,含 50% 变更行
(0.04s→0.18s,掐头去尾失效的真实成本)。

**往返不变量:** inline_diff 的三条路径(词级/句级/difflib 兜底)共用一条不变量:
equal+delete 按序拼接 == 旧文本,equal+insert == 新文本。写成测试后三条路径
谁也逃不掉"丢字/错序"类 bug。

---

## V2-C: 表格结构感知 diff(3.1–3.5)

**3.1 三方案取舍(详见 docs/design-table-diff.md):** 全展平(方案 A)会把一维雪崩
原封不动搬进二维——插一行整行 inserted、插一列全部单元格错位;二维双向 LCS(方案 C)
复杂度 O(m²n²),而真实修订里表格改动 90% 是改单元格和增删行——为一个罕见场景
付四次方,不划算。选了分层对齐(方案 B):表格作为 Block 进文档级 LCS,
配对后再做行级/单元格级,复用 seqdiff 不引入新算法。

**3.3 表格身份配对——没有唯一正确答案的工程判断:** docx 的 `<w:tbl>` 没有 ID,
两份文档里的表格凭什么算同一张?三层策略:表头指纹(Block.key())→ 行级 LCS
覆盖率 → 删表+增表。相似度度量我先试了 Jaccard,但它对行序不敏感——两张
行序完全颠倒的表会判成相同,而表格的行序是有意义的。换成行级 LCS 覆盖率
(2*LCS/(m+n))后行序被计入,还顺手复用了 seqdiff。
阈值用 V2-0 同一套扫参定,不拍脑袋:正样本(改格/插行/移行/插列)最小 0.750,
负样本(无关表/行序颠倒/大部分行重写)最大 0.333,取 0.5 分离干净。
一个值得记的细节:插列样本的行键整体错位,行级覆盖率必然为 0——于是列数不同时
转置到列视角再算一次,列级 LCS 依然保持列序敏感,没有退回 Jaccard。
这就是"边界情况靠度量兜底,而不是靠特判"。

**3.4 行级 LCS + 单元格级 diff 的三个坑:**
1. **表格钩子必须后置。** 表格的身份指纹是首行,改动在数据行时整表 key 不变,
   于是配对上的表格全部以 unchanged 身份落进公共头/尾——中段循环根本看不见它。
   行级 diff 的钩子必须放在 ops 全部组装完之后统一跑一遍(core.py 尾段)。
2. **行移动复用的顺序坑。** detect_moves 认 deleted/inserted 原始键名,先改成
   row_deleted 再检测就一个都匹配不到。正确顺序:diff_ops → detect_moves(原始键名)
   → 最后统一改行级键名。写成回归测试 test_tbl_new3_row_moved。
3. **单格行同位置配对。** 单格行改内容时 row_similarity = 0/1 < 0.5,永远配不上。
   加"新旧位置相同"作第二证据(与 git 的行级直觉一致),但排序时内容证据优先——
   防"删 B 插 X"落在同一位置被误配成改 B→X。
   插列负样本(每行 3→4 列)由列级 LCS 兜底:不崩、每行带 warning 标记列语义漂移。

**3.5 渲染层升级——验证 Part 2.4 的规律:**
上次换 inline 算法(V2-A),渲染层零改动;这次加表格,两个渲染器都得改。
因为 ops 从扁平变嵌套是**结构**变化,解耦保护内容维度、不保护结构维度——
规律成立,写实了。

终端 ASCII 表格的实现要点:
- 标记列的右竖线同时是第一单元格的左竖线,画两遍会出现 `││` 双线(写过测试钉住)。
- 边框段宽和内容宽必须同源计算:单元格 = `│ + 空格 + 内容 + 空格`,边框 = `┬ + (宽+2) 条─`。
- 对齐验证不用人眼:CJK 占 2 格,len() 全错,测试里按显示宽度坐标断言竖线列位置。
- SimSong(截图字体)缺 ⇅/⚠/↷ 字形,表格行移动标记改用 ↑↓,警告用纯文本。
- 行级 warning(列数漂移)由渲染层去重显示;合并单元格看 Block.fmt 的
  unsupported 字段,表格上方一行警告。
**4.2 V2-E 格式变更检测(formatted op + --check-format):**
产品决策:formatted **默认不计入差异判定**(exit 0 + 提示),--check-format 才纳入(exit 1)。
理由:CI 里最常见的诉求是"内容变了吗",格式变更(尤其样式继承导致的)噪声大,
误报代价高;需要严格模式时显式 opt-in。退出码是 CI 的行为契约,不能拍脑袋。

实现的坑:
- **w:sz 单位是半磅**,val=24 是 12pt,不除 2 会和字号直觉对不上。
- **多 run 段落的指纹用多数规则 + mixed 标记**。真实来源:文本改过的段落 Word 会
  拆成多个 run(如 new1 段落三 3 个 run),只加粗部分 run 时指纹如实报 mixed、
  多数 False——这是正确的,错的是样本生成器(整段加粗必须替换段落内所有 rPr,
  段落的 pPr 里也有 rPr 不能动)。
- **并列取先出现**:max(..., key=count) 靠 Python 稳定性保证,别用 set() 去重(哈希序)。
- <w:b w:val="0"/> 是显式关;<w:u val="none"/> 与缺省同为"无下划线"。
- 直接格式化只读 run 的 rPr,**样式继承(styles.xml)是已知边界**:两份文档
  默认样式不同会被漏报,记入 README 边界清单。

指纹对比挂在 core.py 后置钩子里(与表格钩子同位置):unchanged 且指纹不同 →
formatted 并挂 changes;modified 直接挂 changes(文本+格式同时变不产生新 op 类型)。

**6.1 lint/类型检查进 CI——这一天挖出两个真东西:**

1. **ruff --fix 会删 re-export。** align.py 的 `from .seqdiff import diff_ops  # re-export`
   被 F401 当未使用导入自动删掉,core.py 的 `from .align import diff_ops` 当场
   ImportError。教训:re-export 是"看不见的使用",宁可删掉兼容层改直连导入,
   也别让 F401 有机会误杀。以后 --fix 之后必须再跑一遍全量测试。
2. **mypy strict 抓出一个真崩溃。** _fmt_fingerprint 的多数规则用
   `max(dict.fromkeys(values), key=values.count)`,values 里一旦混入 None
   (部分 run 缺 w:sz 的常见情形,如 [12.0, None, None]),max 会对 None 和
   float 做 < 比较——Python 3 直接 TypeError。修法:只比较计数、不比较值,
   顺带保住"并列取先出现"的确定性。写了两个回归测试钉死。
   这是"类型检查不是装饰"的最好证据。

其他决策:
- ops IR 统一成 `Op = dict[str, Any]`(model.py):三层 op 键名各不相同,
  建 TypedDict 联合要 30+ 行还挡不住 JSON 边界,宽松类型是诚实的选择。
- tests/ 加 __init__.py,mypy 的 `tests.*` override 才能命中(无包的脚本
  模块名是文件名本身);tests 放宽四项,experiments 直接 ignore_errors
  (一次性脚本 + matplotlib 桩不全)。
- gen_term_png.py 的 sys.path 引导必须写在 matplotlib import 之前,
  ruff 的 I001 在这里是错的——per-file-ignores 豁免并写明理由。
- 3.10 兼容性本地验证:ast.parse(feature_version=(3,10)) 全量过一遍,
  CI 矩阵防的是运行时行为差异,语法差异本地就能兜。

**6.2 性能基准(手册表格,bench.py 三遍取中位,/usr/bin/time -l):**

| 文档规模 | 变更比例 | 耗时 | 峰值内存 |
|---|---|---|---|
| 100 段 | 5% | 0.02s | 18 MB |
| 500 段 | 5% | 0.04s | 22 MB |
| 2000 段 | 5% | 0.39s | 57 MB |
| 500 段 | 50% | 0.18s | 23 MB |
| 500 段 + 20 表 | 5% | 0.04s | 22 MB |

结论:
- 2000 段 0.39s,远低于 10s 红线,不需要 Hirschberg/array 优化项。
- 50% 变更 0.18s vs 基线 0.04s(4.5×):掐头去尾失效后 LCS 全量起效的
  真实成本,和 README 那句"最坏复杂度不变,降低的是真实输入下的计算量"
  现在有两个数字支撑。
- 20 张表几乎零成本:表格钩子是组装后统一遍历一次,只对配对上的表
  跑行级 diff,与文档规模线性。
- bench 的小坑:docdiff 有差异时退出码是 1,这是正常语义,基准脚本
  必须把 1 当成功(assert returncode in (0,1));/usr/bin/time -l 的
  real 行以 sys 结尾,endswith("real") 解析会永远落空。
